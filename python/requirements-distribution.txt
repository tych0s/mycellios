# PyTorch is installed separately from its CPU wheel index by the setup script.
# accelerate is required only by the native Hugging Face TP/EP loader.
numpy==1.26.4
aiohttp==3.14.1
accelerate==1.14.0
transformers==5.14.1
safetensors==0.8.0
sentencepiece==0.2.2
